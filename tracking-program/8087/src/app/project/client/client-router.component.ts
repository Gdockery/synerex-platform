import { Component } from '@angular/core';

/** Wrapper for /project/client/* child routes (list, create, edit). */
@Component({
  template: '<router-outlet></router-outlet>',
})
export class ClientRouterComponent {}
